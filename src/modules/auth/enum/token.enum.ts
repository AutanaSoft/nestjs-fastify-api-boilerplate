/**
 * This enum defines the different types of tokens that can be used in the authentication module.
 */
export enum TokenType {
  ACCESS = 'access',
  VERIFY_EMAIL = 'verify-email',
  RESET_PASSWORD = 'reset-password',
}
